{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-md-6">
            <h1>Software</h1>
            <table class="table table-stripped">
                <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ software.id }}</td>
                </tr>
                <tr>
                    <th>Hostname</th>
                    <td>{{ software.hostname }}</td>
                </tr>
                <tr>
                    <th>Host_ip</th>
                    <td>{{ software.hostip }}</td>
                </tr>
                <tr>
                    <th>Version</th>
                    <td>{{ software.version }}</td>
                </tr>
                <tr>
                    <th>Modified</th>
                    <td>{% if software.modified %}{{ software.modified|date('Y-m-d H:i:s') }}{% endif %}</td>
                </tr>
                </tbody>
            </table>
        </div>
    </div>
    <div class="row">
        <div class="col-md-10">
            {{ form_start(delete_form) }}
            <div class="btn-group" role="group">
                <a href="{{ path('software_index') }}" class="btn btn-warning"><i class="fa fa-arrow-left"></i> Back to the list</a>
                <a href="{{ path('software_edit', { 'id': software.id }) }}" class="btn btn-success"><i class="fa fa-edit"></i> Edit</a>
                <button class="btn btn-danger" type="submit"><i class="fa fa-trash-o"></i> Delete</button>
            </div>
            {{ form_end(delete_form) }}
        </div>
    </div>
{% endblock %}