{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    <div class="row">
        <div class="col-md-6">
            <h1>Database creation</h1>


            {{ form_widget(form) }}


        </div>
        <div class="col-md-4 text-right">
            <a href="{{ path('database_index') }}">
                <button class="btn btn-warning"><i class="fa fa-arrow-left"></i> Back to the list</button>
            </a>
            <input class="btn btn-success" type="submit" value="Create"/>

        </div>
    </div>
    {{ form_end(form) }}

{% endblock %}
