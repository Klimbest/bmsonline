{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-md-12">
            <h1>Database list</h1>

            <table class="table table-stripped">
                <thead>
                <tr>
                    <th>Id</th>
                    <th>Hostname</th>
                    <th>Host_ip</th>
                    <th>Name</th>
                    <th>Login</th>
                    <th>Password</th>
                    <th>Modified</th>
                    <th>Actions</th>
                </tr>
                </thead>
                <tbody>
                {% for database in databases %}
                    <tr>
                        <td><a href="{{ path('database_show', { 'id': database.id }) }}">{{ database.id }}</a></td>
                        <td>{{ database.hostname }}</td>
                        <td>{{ database.hostip }}</td>
                        <td>{{ database.name }}</td>
                        <td>{{ database.login }}</td>
                        <td>{{ database.password }}</td>
                        <td>{% if database.modified %}{{ database.modified|date('Y-m-d H:i:s') }}{% endif %}</td>
                        <td>
                            <div class="btn-group" role="group">
                                <a href="{{ path('database_show', { 'id': database.id }) }}" class="btn btn-xs btn-primary">Show</a>
                                <a href="{{ path('database_edit', { 'id': database.id }) }}" class="btn btn-xs btn-primary">Edit</a>
                            </div>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

    <div class="row">
        <div class="col-md-10">
            <a href="{{ path('database_new') }}" class="btn btn-success"><i class="fa fa-plus"></i> Add database</a>
        </div>
    </div>
{% endblock %}
